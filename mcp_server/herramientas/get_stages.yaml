name: get_stages
description: "Lista de etapas en un job."
parameters:
  type: object
  properties:
    project:
      type: string
      description: "El proyecto de DataStage."
    job:
      type: string
      description: "El job de DataStage."
  required:
    - project
    - job
returns:
  type: string
  description: "Retorna una lista de etapas en el job especificado."
function: datastage.get_stages