name: get_invocations
description: "Lista de invocaciones de un job."
parameters:
  type: object
  properties:
    project:
      type: string
      description: "El proyecto de DataStage."
    job:
      type: string
      description: "El job de DataStage."
  required:
    - project
    - job
returns:
  type: string
  description: "Retorna una lista de invocaciones del job especificado."
function: datastage.get_invocations