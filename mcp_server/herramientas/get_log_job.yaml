name: get_log_job
description: "Visualizacion de la informacion de log de un job."
parameters:
  type: object
  properties:
    project:
      type: string
      description: "El proyecto de DataStage."
    job:
      type: string
      description: "El job de DataStage."
  required:
    - project
    - job
returns:
  type: string
  description: "Retorna la informacion de log del job especificado."
function: datastage.get_log_job