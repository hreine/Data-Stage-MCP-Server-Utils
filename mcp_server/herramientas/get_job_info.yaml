name: get_job_info
description: "Visualizacion de informacion de un job en particular."
parameters:
  type: object
  properties:
    project:
      type: string
      description: "El proyecto de DataStage."
    job:
      type: string
      description: "El job de DataStage."
  required:
    - project
    - job
returns:
  type: string
  description: "Retorna la informacion del job especificado."
function: datastage.get_job_info