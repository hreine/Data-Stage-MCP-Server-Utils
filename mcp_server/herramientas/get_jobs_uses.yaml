name: get_jobs_uses
description: "Visualizacion de informacion de los jobs que usan un job en particular.|Lista de jobs de los cuales depende un job en particular. "
parameters:
  type: object
  properties:
    project:
      type: string
      description: "El proyecto de DataStage."
    job:
      type: string
      description: "El job de DataStage."
  required:
    - project
    - job
returns:
  type: string
  description: "jobs que usan del job especificado."
function: datastage.get_jobs_uses